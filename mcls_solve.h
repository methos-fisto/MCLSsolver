#ifndef MCLS_SOLVE_H
#define MCLS_SOLVE_H

#include <fstream>
#include <iostream>
#include <string>

#include <glpk.h>

/*
void mcls_solve(const std::string);
void mcls_parse(const std::ifstream&, int&, int&, int*, int*, int*, int*, int*, int**);
glp_prob* mcls_bnb(int const&, int const&, int*&, int*&, int*&, int*&, int*&, int**&);
*/

#endif
